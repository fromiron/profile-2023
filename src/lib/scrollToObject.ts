export default function scrollToObject(event: MouseEvent): void {
  event.preventDefault();

  console.log(event);

  //   if (targetElement) {
  //     const headerHeight: number = 200;

  //     const targetOffset: number = targetElement.offsetTop - headerHeight;

  //     // 스크롤 위치 조정
  //     window.scrollTo({
  //       top: targetOffset,
  //       behavior: "smooth",
  //     });
  //   }
}
